<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sahana_siva_m1.coachnutrition">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BilanActivity" />
        <activity android:name=".ObjectifActivity" />
        <activity android:name=".ListeAlimentsActivity" />
        <activity android:name=".RepasActivity" />

        <provider
            android:name=".CoachNutContentProvider"
            android:authorities="com.sahana_siva_m1.coachnutrition"
            android:enabled="true"
            android:exported="true">
        </provider>
    </application>

</manifest>